const head = React.createElement("h1", { id: "heading", name: "naeee" }, [
  "HEllo Anji this is REact",
  788,
  "okok",
]);

console.log("###", head);
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(head);

console.log("####roor", root);
